id: total_enrollment
label: Import total enrollment
migration_group: pcrc_migration

source:
  plugin: csv
  path: 'modules/custom/pcrc_migrate/csv/total_enrollment.csv'
  header_row_count: 1
  keys:
    - title

process:
  name: title
  vid:
    plugin: default_value
    default_value: total_enrollment

destination:
  plugin: entity:taxonomy_term
