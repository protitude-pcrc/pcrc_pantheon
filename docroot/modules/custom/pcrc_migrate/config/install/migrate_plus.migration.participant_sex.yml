id: participant_sex
label: Import participant sex
migration_group: pcrc_migration

source:
  plugin: csv
  path: 'modules/custom/pcrc_migrate/csv/participant_sex.csv'
  header_row_count: 1
  keys:
    - title

process:
  name: title
  vid:
    plugin: default_value
    default_value: participant_sex

destination:
  plugin: entity:taxonomy_term
