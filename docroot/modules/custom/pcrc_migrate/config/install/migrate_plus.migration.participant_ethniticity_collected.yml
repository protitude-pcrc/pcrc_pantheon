id: participant_ethnicity
label: Import participant ethnicity collected
migration_group: pcrc_migration

source:
  plugin: csv
  path: 'modules/custom/pcrc_migrate/csv/participant_ethnicity_collected.csv'
  header_row_count: 1
  keys:
    - title

process:
  name: title
  vid:
    plugin: default_value
    default_value: participant_ethnicity_collected

destination:
  plugin: entity:taxonomy_term
